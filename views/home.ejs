<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        /* General Body Styles */
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background-color: #f0f2f5;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: flex-start; /* Aligns content to the top */
            min-height: 100vh;
            margin: 0;
            padding: 40px 20px; /* Add some padding for smaller screens */
        }

        /* Container for the main content */
        .container {
            background-color: #ffffff;
            padding: 30px 40px;
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
            width: 100%;
            max-width: 800px;
            text-align: center;
        }

        /* Headings */
        h1 {
            color: #1d2129;
            margin-bottom: 25px;
            font-size: 2.5em;
            font-weight: 600;
        }
        
        h2 {
            font-size: 1.8em;
            border-top: 1px solid #e0e0e0;
            padding-top: 25px;
            margin-top: 35px;
            font-weight: 500;
        }

        /* Form Styling */
        form {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }
        
        label {
            display: none; /* Hiding the label as the placeholder is clear enough */
        }

        /* Input Field */
        #url-input {
            flex-grow: 1; /* Takes up available space */
            padding: 12px 15px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 8px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

        #url-input:focus {
            outline: none;
            border-color: #007bff;
            box-shadow: 0 0 8px rgba(0, 123, 255, 0.3);
        }

        /* Submit Button */
        button[type="submit"] {
            padding: 12px 20px;
            font-size: 16px;
            font-weight: bold;
            color: #fff;
            background: linear-gradient(45deg, #007bff, #0056b3);
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        button[type="submit"]:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(0, 123, 255, 0.4);
        }


        /* Table Styling */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            font-size: 16px;
        }

        thead {
            background-color: #f8f9fa;
        }

        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
            word-break: break-all; /* Prevents long URLs from breaking the layout */
        }

        th {
            font-weight: 600;
            color: #495057;
        }

        /* Hover effect on table rows */
        tbody tr:hover {
            background-color: #e9ecef;
        }

        /* Link Styling */
        a {
            color: #007bff;
            text-decoration: none;
            font-weight: 500;
        }

        a:hover {
            text-decoration: underline;
            color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>URL Shortener 🚀</h1>
        <form action="/url" method="POST">
            <label for="url-input">Enter your URL:</label>
            <input type="text" name="url" id="url-input" placeholder="e.g., https://google.com" required>
            <button type="submit">Shorten</button>
        </form>

        <% if (locals.urls && locals.urls.length > 0) { %>
            <h2>Your Links</h2>
            <table>
                <thead>
                    <tr>
                        <th>S. No</th>
                        <th>Short ID</th>
                        <th>Redirect URL</th>
                        <th>Clicks</th>
                    </tr>
                </thead>
                <tbody>
                    <% urls.forEach((url, index) => { %>
                        <tr>
                            <td><%= index + 1 %></td>
                            <td><a href="/<%= url.shortId %>" target="_blank"><%= url.shortId %></a></td>
                            <td><%= url.redirectURL %></td>
                            <td><%= url.visitHistory.length %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>